1. What makes neural networks more powerful than basic statistical methods like linear regression?

-> Neural networks capture non-linearity much more accurately than linear regression. They do so by applying non-linear activation functions. 

Missed -> neural networks use gradient descent which is not upper-bounded ? 

2. What are the advantages of ReLU activations over sigmoids?

-> Sigmoid makes the values fall in the range of (0,1) and Relu makes the values fall in the range of (0,x). 

Missed -> ReLU avoids vanishing gradient problem compared to sigmoid and is computationally effecient. 

Trace of a matrix - sum of its diagonal 

3. What is the problem in trying to create a neural network using only linear transformations?

-> won't be able to capture information ? Would limit the dimensions of the transformation/ input ? 

4. Rank question 

-> min(n,m,l) rank is upper bounded by row or columnar space 

5. Expected value 

-> E(X+Y) = nu1 + nu2 